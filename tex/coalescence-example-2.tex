\begin{figure}[H]
    \def\x{1.0cm}
    \def\y{0.3cm}
    \centering
    \begin{subfigure}{0.3\linewidth}
        \begin{tikzpicture}[transform shape, label distance=5mm, every path/.style={color=black!25}, every node/.style={circle, fill=black!25, inner sep=0.05cm}, every label/.append style={color=black!100}]
            % nodes
            % top
            \node[on grid, anchor=center, label=left:\rotatebox{-90}{$\neg a$}, label=above:{$a$}, style={color=black!100}](aa){};
            \node[on grid, right=\x of aa, label=above:{$\vee$}](av){};
            \node[on grid, right=\x of av, label=above:{$\neg a$}](ana){};
            % middle
            \node[on grid, below=\x of aa, label=left:\rotatebox{-90}{$\wedge$}](va){};
            \node[on grid, right=\x of va](vv){};
            \node[on grid, right=\x of vv](vna){};
            % bottom
            \node[on grid, below=\x of va, label=left:\rotatebox{-90}{$a$}](naa){};
            \node[on grid, right=\x of naa](nav){};
            \node[on grid, right=\x of nav, style={color=black!100}](nana){};
            % disjunction helpers
            \coordinate[on grid, right=\y of va](da);
            \coordinate[on grid, right=\y of vv](dv);
            \coordinate[on grid, right=\y of vna](dna);

            % edges
            % horizontal
            \draw[-arr] (aa) to [bend left] (av);
            \draw[-arr] (ana) to [bend right] (av);
            \draw[-arr] (va) to [bend left] (vv);
            \draw[-arr] (vna) to [bend right] (vv);
            \draw[-arr] (naa) to [bend left] (nav);
            \draw[-arr] (nana) to [bend right] (nav);
            % vertical
            \draw[-] (aa) to [bend left] (naa);
            \draw[-arr] (da) to (va);
            \draw[-] (av) to [bend left] (nav);
            \draw[-arr] (dv) to (vv);
            \draw[-] (ana) to [bend left] (nana);
            \draw[-arr] (dna) to (vna);
        \end{tikzpicture}
    \end{subfigure}
    $\leadsto$
    \begin{subfigure}{0.3\linewidth}
        \begin{tikzpicture}[transform shape, label distance=5mm, every path/.style={color=black!25}, every node/.style={circle, fill=black!25, inner sep=0.05cm}, every label/.append style={color=black!100}]
            % nodes
            % top
            \node[on grid, anchor=center, label=left:\rotatebox{-90}{$\neg a$}, label=above:{$a$}, style={color=black!100}](aa){};
            \node[on grid, right=\x of aa, label=above:{$\vee$}, style={color=black!100}](av){};
            \node[on grid, right=\x of av, label=above:{$\neg a$}](ana){};
            % middle
            \node[on grid, below=\x of aa, label=left:\rotatebox{-90}{$\wedge$}](va){};
            \node[on grid, right=\x of va](vv){};
            \node[on grid, right=\x of vv](vna){};
            % bottom
            \node[on grid, below=\x of va, label=left:\rotatebox{-90}{$a$}](naa){};
            \node[on grid, right=\x of naa, style={color=black!100}](nav){};
            \node[on grid, right=\x of nav, style={color=black!100}](nana){};
            % disjunction helpers
            \coordinate[on grid, right=\y of va](da);
            \coordinate[on grid, right=\y of vv](dv);
            \coordinate[on grid, right=\y of vna](dna);

            % edges
            % horizontal
            \draw[-arr, style={color=black!100}] (aa) to [bend left] (av);
            \draw[-arr] (ana) to [bend right] (av);
            \draw[-arr] (va) to [bend left] (vv);
            \draw[-arr] (vna) to [bend right] (vv);
            \draw[-arr] (naa) to [bend left] (nav);
            \draw[-arr, style={color=black!100}] (nana) to [bend right] (nav);
            % vertical
            \draw[-] (aa) to [bend left] (naa);
            \draw[-arr] (da) to (va);
            \draw[-] (av) to [bend left] (nav);
            \draw[-arr] (dv) to (vv);
            \draw[-] (ana) to [bend left] (nana);
            \draw[-arr] (dna) to (vna);
        \end{tikzpicture}
    \end{subfigure}
    $\leadsto$
    \begin{subfigure}{0.3\linewidth}
        \begin{tikzpicture}[transform shape, label distance=5mm, every path/.style={color=black!25}, every node/.style={circle, fill=black!25, inner sep=0.05cm}, every label/.append style={color=black!100}]
            % nodes
            % top
            \node[on grid, anchor=center, label=left:\rotatebox{-90}{$\neg a$}, label=above:{$a$}, style={color=black!100}](aa){};
            \node[on grid, right=\x of aa, label=above:{$\vee$}, style={color=black!100}](av){};
            \node[on grid, right=\x of av, label=above:{$\neg a$}](ana){};
            % middle
            \node[on grid, below=\x of aa, label=left:\rotatebox{-90}{$\wedge$}](va){};
            \node[on grid, right=\x of va, style={color=black!100}](vv){};
            \node[on grid, right=\x of vv](vna){};
            % bottom
            \node[on grid, below=\x of va, label=left:\rotatebox{-90}{$a$}](naa){};
            \node[on grid, right=\x of naa, style={color=black!100}](nav){};
            \node[on grid, right=\x of nav, style={color=black!100}](nana){};
            % disjunction helpers
            \coordinate[on grid, right=\y of va](da);
            \coordinate[on grid, right=\y of vv](dv);
            \coordinate[on grid, right=\y of vna](dna);

            % edges
            % horizontal
            \draw[-arr, style={color=black!100}] (aa) to [bend left] (av);
            \draw[-arr] (ana) to [bend right] (av);
            \draw[-arr] (va) to [bend left] (vv);
            \draw[-arr] (vna) to [bend right] (vv);
            \draw[-arr] (naa) to [bend left] (nav);
            \draw[-arr, style={color=black!100}] (nana) to [bend right] (nav);
            % vertical
            \draw[-] (aa) to [bend left] (naa);
            \draw[-arr] (da) to (va);
            \draw[-, style={color=black!100}] (av) to [bend left] (nav);
            \draw[-arr, style={color=black!100}] (dv) to (vv);
            \draw[-] (ana) to [bend left] (nana);
            \draw[-arr] (dna) to (vna);
        \end{tikzpicture}
    \end{subfigure}
\end{figure}

